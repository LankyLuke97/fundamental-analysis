# use == version_num to specify a specific version,
# >= or <= to specify a range of version numbers,
# or simply the module name to keep up to date

alembic
fastapi
httpx
mypy
numpy
pandas
psycopg
python-dateutil
python-dotenv
ruff
sqlmodel
uvicorn[standard]

